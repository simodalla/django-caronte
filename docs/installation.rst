============
Installation
============

At the command line::

    $ easy_install django-caronte

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-caronte
    $ pip install django-caronte
